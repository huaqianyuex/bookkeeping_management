# 个人记账本 API 接口文档

## 项目信息

- 基础地址：`http://localhost:8080`
- 数据格式：JSON
- 认证方式：JWT Token（放在请求头 `Authorization` 中，格式 `Bearer {token}`）

## 后端技术栈建议

| 技术 | 用途 |
|------|------|
| Spring Boot 3.x | 框架 |
| Spring Web (spring-boot-starter-web) | REST API |
| Spring Security | 安全认证 |
| MyBatis-Plus | ORM |
| MySQL 8.0 | 数据库 |
| JJWT / Auth0 java-jwt | JWT 生成与校验 |
| Lombok | 简化代码 |
| Hibernate Validator | 参数校验 |

---

## 统一响应格式

### 标准响应

| 字段 | 类型 | 说明 |
|------|------|------|
| code | Integer | 200 成功 / 400 参数错误 / 401 未登录 / 500 服务器错误 |
| message | String | 提示信息 |
| data | Object | 返回数据，失败时为 null |

```json
{
    "code": 200,
    "message": "操作成功",
    "data": {}
}
```

### 分页响应

| 字段 | 类型 | 说明 |
|------|------|------|
| records | Array | 当前页数据列表 |
| total | Long | 总记录数 |
| pages | Long | 总页数 |
| current | Long | 当前页码 |
| size | Long | 每页条数 |

> 分页查询接口统一返回此格式，前端使用 MyBatis-Plus `Page` 分页插件。

---

## 接口总览

| # | 模块 | 接口 | 方法 | 路径 | 需登录 | 练习阶段 |
|---|------|------|------|------|--------|----------|
| 1 | 用户 | 注册 | POST | `/api/user/register` | 否 | 一 |
| 2 | 用户 | 登录 | POST | `/api/user/login` | 否 | 一 |
| 3 | 用户 | 个人信息 | GET | `/api/user/info` | 是 | 一 |
| 4 | 用户 | 修改密码 | PUT | `/api/user/password` | 是 | 一 |
| 5 | 分类 | 查询列表 | GET | `/api/categories` | 是 | 一 |
| 6 | 分类 | 新增 | POST | `/api/categories` | 是 | 一 |
| 7 | 分类 | 修改 | PUT | `/api/categories/{id}` | 是 | 一 |
| 8 | 分类 | 删除 | DELETE | `/api/categories/{id}` | 是 | 一 |
| 9 | 账单 | 分页查询 | GET | `/api/records` | 是 | 二 |
| 10 | 账单 | 查看详情 | GET | `/api/records/{id}` | 是 | 二 |
| 11 | 账单 | 新增 | POST | `/api/records` | 是 | 二 |
| 12 | 账单 | 修改 | PUT | `/api/records/{id}` | 是 | 二 |
| 13 | 账单 | 删除 | DELETE | `/api/records/{id}` | 是 | 二 |
| 14 | 统计 | 月度汇总 | GET | `/api/statistics/monthly` | 是 | 三 |
| 15 | 统计 | 分类统计 | GET | `/api/statistics/category` | 是 | 三 |
| 16 | 管理员 | 用户列表 | GET | `/api/admin/users` | 是(管理员) | - |
| 17 | 管理员 | 用户详情 | GET | `/api/admin/users/{id}` | 是(管理员) | - |
| 18 | 管理员 | 删除用户 | DELETE | `/api/admin/users/{id}` | 是(管理员) | - |
| 19 | 管理员 | 批量删除用户 | POST | `/api/admin/users/batch-delete` | 是(管理员) | - |
| 20 | 管理员 | 全量账单 | GET | `/api/admin/records` | 是(管理员) | - |
| 21 | 管理员 | 批量删除账单 | POST | `/api/admin/records/batch-delete` | 是(管理员) | - |
| 22 | 管理员 | 系统概览 | GET | `/api/admin/statistics/overview` | 是(管理员) | - |

---

## 一、用户模块 `/api/user`

### 1.1 用户注册

- **请求方式**：POST
- **路径**：`/api/user/register`
- **是否需要登录**：否
- **练习重点**：参数校验、密码加密、唯一性检查

#### 请求体

| 字段 | 类型 | 必填 | 校验规则 |
|------|------|------|----------|
| username | String | 是 | 2-20位，不能重复 |
| password | String | 是 | 6-20位 |

```json
{
    "username": "zhangsan",
    "password": "123456"
}
```

#### 响应

```json
{
    "code": 200,
    "message": "注册成功",
    "data": {
        "id": 1,
        "username": "zhangsan"
    }
}
```

#### 错误场景

| 场景 | HTTP 状态码 | code | message |
|------|-------------|------|---------|
| 用户名已存在 | 500 | 500 | 用户名已存在 |
| 用户名为空 | 400 | 400 | 用户名不能为空 |
| 密码为空 | 400 | 400 | 密码不能为空 |
| 用户名长度不足 | 400 | 400 | 用户名长度需要在2-20位之间 |
| 密码长度不足 | 400 | 400 | 密码长度需要在6-20位之间 |

---

### 1.2 用户登录

- **请求方式**：POST
- **路径**：`/api/user/login`
- **是否需要登录**：否
- **练习重点**：密码校验、JWT Token 生成

#### 请求体

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | String | 是 | 用户名 |
| password | String | 是 | 密码 |

```json
{
    "username": "zhangsan",
    "password": "123456"
}
```

#### 响应

```json
{
    "code": 200,
    "message": "登录成功",
    "data": "eyJhbGciOiJIUzI1NiJ9.xxxxx"
}
```

> `data` 字段直接返回 Token 字符串（非对象），前端会存入 localStorage 并在后续请求的 `Authorization` 头中使用。

#### 错误场景

| 场景 | HTTP 状态码 | code | message |
|------|-------------|------|---------|
| 用户不存在 | 500 | 500 | 用户不存在 |
| 密码错误 | 500 | 500 | 密码错误 |

---

### 1.3 查看个人信息

- **请求方式**：GET
- **路径**：`/api/user/info`
- **是否需要登录**：是
- **练习重点**：从 Token 中提取用户 ID、JWT 拦截器
- **请求头**：`Authorization: Bearer {token}`

#### 请求参数

无

#### 响应

```json
{
    "code": 200,
    "message": "操作成功",
    "data": {
        "id": 1,
        "username": "zhangsan",
        "createTime": "2026-05-12T11:30:00",
        "updateTime": "2026-05-12T11:30:00"
    }
}
```

#### 错误场景

| 场景 | HTTP 状态码 | code | message |
|------|-------------|------|---------|
| 未登录 | 401 | 401 | 未登录 |
| Token 过期/无效 | 401 | 401 | token无效或已过期 |

---

### 1.4 修改密码

- **请求方式**：PUT
- **路径**：`/api/user/password`
- **是否需要登录**：是
- **练习重点**：原密码校验、密码更新事务
- **请求头**：`Authorization: Bearer {token}`

#### 请求体

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| oldPassword | String | 是 | 原密码 |
| newPassword | String | 是 | 新密码，6-20位 |

```json
{
    "oldPassword": "123456",
    "newPassword": "654321"
}
```

#### 响应

```json
{
    "code": 200,
    "message": "修改成功",
    "data": null
}
```

#### 错误场景

| 场景 | HTTP 状态码 | code | message |
|------|-------------|------|---------|
| 原密码错误 | 500 | 500 | 原密码不正确 |

---

## 二、分类模块 `/api/categories`

> 分类是用户维度的，每个用户只能看到和操作自己的分类。

### 2.1 查询分类列表

- **请求方式**：GET
- **路径**：`/api/categories`
- **是否需要登录**：是
- **练习重点**：按用户 ID 过滤、可选参数查询

#### 请求参数

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| type | Integer | 否 | 0=支出 / 1=收入，不传查全部 |

#### 响应

```json
{
    "code": 200,
    "message": "操作成功",
    "data": [
        {
            "id": 1,
            "name": "餐饮",
            "type": 0,
            "userId": 1,
            "createTime": "2026-05-12T11:30:00",
            "updateTime": "2026-05-12T11:30:00"
        }
    ]
}
```

#### CategoryVO 结构

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 分类ID |
| name | String | 分类名称 |
| type | Integer | 0=支出 / 1=收入 |
| userId | Long | 所属用户ID |
| createTime | LocalDateTime | 创建时间，格式 `yyyy-MM-dd'T'HH:mm:ss` |
| updateTime | LocalDateTime | 更新时间 |

---

### 2.2 新增分类

- **请求方式**：POST
- **路径**：`/api/categories`
- **是否需要登录**：是
- **练习重点**：DTO 参数校验、自动填充 userId

#### 请求体

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | String | 是 | 分类名称，1-20位 |
| type | Integer | 是 | 0=支出 / 1=收入 |

```json
{
    "name": "餐饮",
    "type": 0
}
```

#### 响应

```json
{
    "code": 200,
    "message": "新增成功",
    "data": {
        "id": 13,
        "name": "餐饮",
        "type": 0,
        "userId": 1,
        "createTime": "2026-05-12T11:30:00",
        "updateTime": "2026-05-12T11:30:00"
    }
}
```

#### 错误场景

| 场景 | code | message |
|------|------|---------|
| 名称为空 | 400 | 分类名称不能为空 |
| 类型为空 | 400 | 分类类型不能为空 |

---

### 2.3 修改分类

- **请求方式**：PUT
- **路径**：`/api/categories/{id}`
- **是否需要登录**：是
- **练习重点**：路径参数、资源归属校验

#### 路径参数

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 分类ID |

#### 请求体

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | String | 是 | 分类名称 |
| type | Integer | 是 | 0=支出 / 1=收入 |

```json
{
    "name": "吃饭",
    "type": 0
}
```

#### 响应

同 2.2 返回结构。

```json
{
    "code": 200,
    "message": "修改成功",
    "data": {
        "id": 1,
        "name": "吃饭",
        "type": 0,
        "userId": 1,
        "createTime": "2026-05-12T11:30:00",
        "updateTime": "2026-05-12T11:35:00"
    }
}
```

#### 错误场景

| 场景 | code | message |
|------|------|---------|
| 分类不存在 | 500 | 分类不存在 |
| 不是自己的分类 | 500 | 分类不存在（与不存在统一提示，防止信息泄露） |

---

### 2.4 删除分类

- **请求方式**：DELETE
- **路径**：`/api/categories/{id}`
- **是否需要登录**：是
- **练习重点**：资源归属校验、级联处理（关联账单如何处理？）

#### 路径参数

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 分类ID |

#### 响应

```json
{
    "code": 200,
    "message": "删除成功",
    "data": null
}
```

#### 错误场景

| 场景 | code | message |
|------|------|---------|
| 分类不存在 | 500 | 分类不存在 |
| 不是自己的分类 | 500 | 分类不存在 |

> 💡 **思考题**：如果该分类下已有账单记录，应该阻止删除还是允许删除并将关联记录的 categoryId 置空？

---

## 三、账单记录模块 `/api/records`

### 3.1 分页查询账单

- **请求方式**：GET
- **路径**：`/api/records`
- **是否需要登录**：是
- **练习重点**：MyBatis-Plus 分页、联表查询、条件筛选

#### 请求参数

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | Integer | 否 | 页码，默认 1 |
| size | Integer | 否 | 每页条数，默认 10 |
| categoryId | Long | 否 | 按分类筛选 |
| month | String | 否 | 按月份筛选，格式 `"2026-05"` |

#### 响应（分页格式）

```json
{
    "code": 200,
    "message": "操作成功",
    "data": {
        "records": [
            {
                "id": 1,
                "userId": 1,
                "categoryId": 1,
                "categoryName": "餐饮",
                "categoryType": 0,
                "amount": 35.50,
                "remark": "午饭",
                "recordDate": "2026-05-12",
                "createTime": "2026-05-12T12:00:00",
                "updateTime": "2026-05-12T12:00:00"
            }
        ],
        "total": 50,
        "pages": 5,
        "current": 1,
        "size": 10
    }
}
```

#### RecordVO 结构

> ⚠️ **注意**：`categoryName` 和 `categoryType` 来自分类表，需要通过联表查询获取，不是记录表本身的字段。

| 字段 | 类型 | 来源 | 说明 |
|------|------|------|------|
| id | Long | record 表 | 记录ID |
| userId | Long | record 表 | 用户ID |
| categoryId | Long | record 表 | 分类ID |
| categoryName | String | category 表（联表） | 分类名称 |
| categoryType | Integer | category 表（联表） | 0=支出 / 1=收入 |
| amount | BigDecimal | record 表 | 金额 |
| remark | String | record 表 | 备注，可能为 null |
| recordDate | LocalDate | record 表 | 记录日期，格式 `yyyy-MM-dd` |
| createTime | LocalDateTime | record 表 | 创建时间 |
| updateTime | LocalDateTime | record 表 | 更新时间 |

#### SQL 参考（MyBatis-Plus 联表查询）

```xml
<select id="selectRecordVOPage" resultType="com.example.test_backend.entity.Vo.RecordVO">
    SELECT r.*, c.name AS categoryName, c.type AS categoryType
    FROM record r
    LEFT JOIN category c ON r.category_id = c.id
    WHERE r.user_id = #{userId}
    <if test="categoryId != null"> AND r.category_id = #{categoryId} </if>
    <if test="month != null"> AND DATE_FORMAT(r.record_date, '%Y-%m') = #{month} </if>
    ORDER BY r.record_date DESC, r.create_time DESC
</select>
```

---

### 3.2 查看账单详情

- **请求方式**：GET
- **路径**：`/api/records/{id}`
- **是否需要登录**：是
- **练习重点**：按 ID 查询 + 联表获取分类信息

#### 路径参数

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 记录ID |

#### 响应

同 3.1 的 RecordVO 结构。

```json
{
    "code": 200,
    "message": "操作成功",
    "data": {
        "id": 1,
        "userId": 1,
        "categoryId": 1,
        "categoryName": "餐饮",
        "categoryType": 0,
        "amount": 35.50,
        "remark": "午饭",
        "recordDate": "2026-05-12",
        "createTime": "2026-05-12T12:00:00",
        "updateTime": "2026-05-12T12:00:00"
    }
}
```

#### 错误场景

| 场景 | code | message |
|------|------|---------|
| 记录不存在 | 500 | 记录不存在 |
| 不是自己的记录 | 500 | 记录不存在 |

---

### 3.3 新增账单

- **请求方式**：POST
- **路径**：`/api/records`
- **是否需要登录**：是
- **练习重点**：外键校验（分类必须属于当前用户）

#### 请求体

| 字段 | 类型 | 必填 | 校验规则 |
|------|------|------|----------|
| categoryId | Long | 是 | 必须存在且属于当前用户 |
| amount | BigDecimal | 是 | 必须大于 0 |
| remark | String | 否 | 最多 200 字 |
| recordDate | String | 是 | 格式 `yyyy-MM-dd`，不能为未来日期？ |

```json
{
    "categoryId": 1,
    "amount": 35.50,
    "remark": "午饭",
    "recordDate": "2026-05-12"
}
```

#### 响应

```json
{
    "code": 200,
    "message": "新增成功",
    "data": {
        "id": 51,
        "userId": 1,
        "categoryId": 1,
        "categoryName": "餐饮",
        "categoryType": 0,
        "amount": 35.50,
        "remark": "午饭",
        "recordDate": "2026-05-12",
        "createTime": "2026-05-12T12:00:00",
        "updateTime": "2026-05-12T12:00:00"
    }
}
```

#### 错误场景

| 场景 | code | message |
|------|------|---------|
| 分类不存在 | 500 | 分类不存在 |
| 金额为空 | 400 | 金额不能为空 |
| 金额不大于0 | 400 | 金额必须大于0 |
| 日期为空 | 400 | 日期不能为空 |
| 日期格式错误 | 400 | 日期格式错误 |

---

### 3.4 修改账单

- **请求方式**：PUT
- **路径**：`/api/records/{id}`
- **是否需要登录**：是
- **练习重点**：资源归属校验、外键校验

#### 路径参数

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 记录ID |

#### 请求体

同 3.3。

```json
{
    "categoryId": 1,
    "amount": 40.00,
    "remark": "午饭加饮料",
    "recordDate": "2026-05-12"
}
```

#### 响应

```json
{
    "code": 200,
    "message": "修改成功",
    "data": {
        "id": 1,
        "userId": 1,
        "categoryId": 1,
        "categoryName": "餐饮",
        "categoryType": 0,
        "amount": 40.00,
        "remark": "午饭加饮料",
        "recordDate": "2026-05-12",
        "createTime": "2026-05-12T12:00:00",
        "updateTime": "2026-05-12T12:05:00"
    }
}
```

#### 错误场景

| 场景 | code | message |
|------|------|---------|
| 记录不存在 | 500 | 记录不存在 |
| 不是自己的记录 | 500 | 记录不存在 |
| 分类不存在 | 500 | 分类不存在 |

---

### 3.5 删除账单

- **请求方式**：DELETE
- **路径**：`/api/records/{id}`
- **是否需要登录**：是
- **练习重点**：资源归属校验

#### 路径参数

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 记录ID |

#### 响应

```json
{
    "code": 200,
    "message": "删除成功",
    "data": null
}
```

#### 错误场景

| 场景 | code | message |
|------|------|---------|
| 记录不存在 | 500 | 记录不存在 |
| 不是自己的记录 | 500 | 记录不存在 |

---

## 四、统计模块 `/api/statistics`

### 4.1 月度收支汇总

- **请求方式**：GET
- **路径**：`/api/statistics/monthly`
- **是否需要登录**：是
- **练习重点**：SQL 聚合查询（SUM + CASE WHEN）

#### 请求参数

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| year | Integer | 是 | 年份，如 2026 |
| month | Integer | 是 | 月份，如 5 |

#### SQL 参考

```sql
SELECT
    #{year} AS year,
    #{month} AS month,
    COALESCE(SUM(CASE WHEN c.type = 1 THEN r.amount ELSE 0 END), 0) AS totalIncome,
    COALESCE(SUM(CASE WHEN c.type = 0 THEN r.amount ELSE 0 END), 0) AS totalExpense
FROM record r
LEFT JOIN category c ON r.category_id = c.id
WHERE r.user_id = #{userId}
  AND YEAR(r.record_date) = #{year}
  AND MONTH(r.record_date) = #{month}
```

#### 响应

```json
{
    "code": 200,
    "message": "操作成功",
    "data": {
        "year": 2026,
        "month": 5,
        "totalIncome": 8000.00,
        "totalExpense": 3500.00,
        "balance": 4500.00
    }
}
```

> `balance`（结余）= `totalIncome - totalExpense`，可在 Java 代码中计算。

---

### 4.2 分类统计

- **请求方式**：GET
- **路径**：`/api/statistics/category`
- **是否需要登录**：是
- **练习重点**：联表查询 + GROUP BY + 百分比计算

#### 请求参数

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| year | Integer | 是 | 年份 |
| month | Integer | 是 | 月份 |
| type | Integer | 否 | 0=支出 / 1=收入，默认 0 |

#### SQL 参考

```sql
SELECT
    c.id AS categoryId,
    c.name AS categoryName,
    COALESCE(SUM(r.amount), 0) AS amount
FROM record r
LEFT JOIN category c ON r.category_id = c.id
WHERE r.user_id = #{userId}
  AND YEAR(r.record_date) = #{year}
  AND MONTH(r.record_date) = #{month}
  AND c.type = #{type}
GROUP BY c.id, c.name
ORDER BY amount DESC
```

> `percentage` 在 Java 中计算：`amount / 分类总金额 * 100`，保留一位小数。

#### 响应

```json
{
    "code": 200,
    "message": "操作成功",
    "data": [
        {
            "categoryId": 1,
            "categoryName": "餐饮",
            "amount": 1200.00,
            "percentage": 34.2
        },
        {
            "categoryId": 2,
            "categoryName": "交通",
            "amount": 800.00,
            "percentage": 22.8
        }
    ]
}
```

---

## 五、管理员模块 `/api/admin`

> 管理员接口，需登录且 `role=1`。非管理员访问返回 `403`。

### 5.1 用户列表

- **请求方式**：GET
- **路径**：`/api/admin/users`
- **是否需要登录**：是（管理员）
- **请求头**：`Authorization: Bearer {token}`

#### 请求参数

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | Integer | 否 | 页码，默认 1 |
| size | Integer | 否 | 每页条数，默认 10 |
| keyword | String | 否 | 按用户名模糊搜索 |

#### 响应

```json
{
    "code": 200,
    "message": "操作成功",
    "data": {
        "records": [
            {
                "id": 1,
                "username": "zhangsan",
                "role": 0,
                "createTime": "2026-05-12T11:30:00",
                "updateTime": "2026-05-12T11:30:00"
            }
        ],
        "total": 20,
        "pages": 2,
        "current": 1,
        "size": 10
    }
}
```

---

### 5.2 用户详情

- **请求方式**：GET
- **路径**：`/api/admin/users/{id}`
- **是否需要登录**：是（管理员）

#### 响应

```json
{
    "code": 200,
    "message": "操作成功",
    "data": {
        "id": 1,
        "username": "zhangsan",
        "role": 0,
        "createTime": "2026-05-12T11:30:00",
        "updateTime": "2026-05-12T11:30:00"
    }
}
```

---

### 5.3 删除用户

- **请求方式**：DELETE
- **路径**：`/api/admin/users/{id}`
- **是否需要登录**：是（管理员）
- **说明**：删除用户及其所有账单和分类

#### 响应

```json
{
    "code": 200,
    "message": "删除成功",
    "data": null
}
```

#### 错误场景

| 场景 | code | message |
|------|------|---------|
| 用户不存在 | 500 | 用户不存在 |
| 不能删除管理员 | 500 | 不能删除管理员账户 |

---

### 5.4 批量删除用户

- **请求方式**：POST
- **路径**：`/api/admin/users/batch-delete`
- **是否需要登录**：是（管理员）
- **说明**：批量删除用户及其所有账单和分类

#### 请求体

```json
{
    "ids": [2, 3, 5]
}
```

#### 响应

```json
{
    "code": 200,
    "message": "批量删除成功",
    "data": null
}
```

---

### 5.5 全量账单列表

- **请求方式**：GET
- **路径**：`/api/admin/records`
- **是否需要登录**：是（管理员）

#### 请求参数

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | Integer | 否 | 页码，默认 1 |
| size | Integer | 否 | 每页条数，默认 10 |
| type | Integer | 否 | 0=支出 / 1=收入，筛选类型 |
| month | String | 否 | 按月份筛选，格式 `"2026-05"` |
| userId | Long | 否 | 按用户ID筛选，查看某用户的所有账单 |

#### 响应

```json
{
    "code": 200,
    "message": "操作成功",
    "data": {
        "records": [
            {
                "id": 1,
                "userId": 2,
                "username": "zhangsan",
                "categoryId": 1,
                "categoryName": "餐饮",
                "categoryType": 0,
                "amount": 35.50,
                "remark": "午饭",
                "recordDate": "2026-05-12",
                "createTime": "2026-05-12T12:00:00"
            }
        ],
        "total": 500,
        "pages": 50,
        "current": 1,
        "size": 10
    }
}
```

---

### 5.6 批量删除账单

- **请求方式**：POST
- **路径**：`/api/admin/records/batch-delete`
- **是否需要登录**：是（管理员）

#### 请求体

```json
{
    "ids": [1, 5, 12]
}
```

#### 响应

```json
{
    "code": 200,
    "message": "批量删除成功",
    "data": null
}
```

---

### 5.7 系统概览统计

- **请求方式**：GET
- **路径**：`/api/admin/statistics/overview`
- **是否需要登录**：是（管理员）

#### 响应

```json
{
    "code": 200,
    "message": "操作成功",
    "data": {
        "totalUsers": 120,
        "totalRecords": 3500,
        "totalCategories": 480,
        "totalIncome": 960000.00,
        "totalExpense": 620000.00
    }
}
```

---

## 六、数据库设计

### 建表语句

```sql
-- 用户表
CREATE TABLE user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    password VARCHAR(100) NOT NULL COMMENT '密码（加密存储）',
    role TINYINT NOT NULL DEFAULT 0 COMMENT '角色：0=普通用户，1=管理员',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_username (username)
) COMMENT '用户表';

-- 分类表
CREATE TABLE category (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '分类ID',
    name VARCHAR(50) NOT NULL COMMENT '分类名称',
    type TINYINT NOT NULL COMMENT '类型：0=支出，1=收入',
    user_id BIGINT NOT NULL COMMENT '所属用户ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_user_id (user_id)
) COMMENT '分类表';

-- 账单记录表
CREATE TABLE record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '记录ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    category_id BIGINT NOT NULL COMMENT '分类ID',
    amount DECIMAL(10, 2) NOT NULL COMMENT '金额',
    remark VARCHAR(200) DEFAULT NULL COMMENT '备注',
    record_date DATE NOT NULL COMMENT '记录日期',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_user_date (user_id, record_date),
    INDEX idx_category (category_id)
) COMMENT '账单记录表';
```

### 关系说明

- `category.user_id` → `user.id`（多对一）
- `record.user_id` → `user.id`（多对一）
- `record.category_id` → `category.id`（多对一）
- 所有数据按 `user_id` 隔离，用户只能操作自己的数据

---

## 七、推荐学习路线（三阶段）

### 阶段一：基础 CRUD（用户 + 分类）

> 目标：掌握 Spring Boot 项目搭建、MyBatis-Plus CRUD、JWT 认证

| 步骤 | 内容 | 涉及技术 |
|------|------|----------|
| 1 | 初始化 Spring Boot 项目 + 配置数据源 | Spring Initializr, application.yml |
| 2 | 建表 + 创建 Entity / Mapper | MyBatis-Plus |
| 3 | 实现用户注册（密码加密） | BCryptPasswordEncoder |
| 4 | 实现用户登录（生成 JWT） | JJWT 库 |
| 5 | 实现 JWT 拦截器（校验 Token + 提取用户 ID） | HandlerInterceptor |
| 6 | 实现用户信息 + 修改密码接口 | Spring Security |
| 7 | 实现分类 CRUD（归属校验） | MyBatis-Plus CRUD |

### 阶段二：账单管理 + 分页

> 目标：掌握分页查询、联表查询、参数校验

| 步骤 | 内容 | 涉及技术 |
|------|------|----------|
| 8 | 实现账单新增（外键校验） | Hibernate Validator |
| 9 | 实现账单修改/删除（归属校验） | - |
| 10 | 实现账单分页查询（多条件筛选） | MyBatis-Plus Page + 自定义 XML |
| 11 | 实现账单详情查询 | 联表查询 |

### 阶段三：统计分析

> 目标：掌握 SQL 聚合函数、GROUP BY、数据处理

| 步骤 | 内容 | 涉及技术 |
|------|------|----------|
| 12 | 实现月度收支汇总 | SUM + CASE WHEN |
| 13 | 实现分类统计 + 百分比 | GROUP BY + Java 计算 |

---

## 八、项目目录结构（参考）

```
test_backend/
├── src/main/java/com/example/test_backend/
│   ├── TestBackendApplication.java
│   ├── common/                    # 通用类
│   │   ├── Result.java            # 统一响应封装
│   │   ├── PageResult.java        # 分页结果封装
│   │   └── GlobalExceptionHandler.java  # 全局异常处理
│   ├── config/                    # 配置
│   │   ├── WebConfig.java         # CORS 跨域配置
│   │   ├── SecurityConfig.java    # 密码编码器配置
│   │   └── MyBatisPlusConfig.java # 分页插件
│   ├── interceptor/               # 拦截器
│   │   ├── LoginInterceptor.java  # JWT 登录校验拦截器
│   │   └── UserContext.java       # 线程变量存储用户ID
│   ├── util/
│   │   └── JwtUtil.java           # JWT 工具类（生成 + 解析）
│   ├── entity/                    # 数据实体
│   │   ├── User.java
│   │   ├── Category.java
│   │   └── Record.java
│   ├── entity/Vo/                 # 视图对象（返回给前端）
│   │   ├── UserVO.java
│   │   ├── RecordVO.java
│   │   ├── MonthlyStatisticsVO.java
│   │   └── CategoryStatisticsVO.java
│   ├── dto/                       # 数据传输对象（前端 → 后端）
│   │   ├── RegisterDTO.java
│   │   ├── LoginDTO.java
│   │   ├── PasswordDTO.java
│   │   ├── CategoryDTO.java
│   │   └── RecordDTO.java
│   ├── mapper/                    # MyBatis-Plus Mapper
│   │   ├── UserMapper.java
│   │   ├── CategoryMapper.java
│   │   └── RecordMapper.java
│   ├── service/                   # 业务接口
│   │   ├── UserService.java
│   │   ├── CategoryService.java
│   │   ├── RecordService.java
│   │   └── StatisticsService.java
│   └── service/impl/              # 业务实现
│       ├── UserServiceImpl.java
│       ├── CategoryServiceImpl.java
│       ├── RecordServiceImpl.java
│       └── StatisticsServiceImpl.java
├── src/main/resources/
│   ├── application.yml
│   └── mapper/                    # MyBatis XML 映射文件（可选）
│       ├── RecordMapper.xml
│       └── ...
└── pom.xml
```

---

## 九、DTO / VO 说明

```
DTO（Data Transfer Object，前端 → 后端）
  RegisterDTO:     { username, password }
  LoginDTO:        { username, password }
  CategoryDTO:     { name, type }
  RecordDTO:       { categoryId, amount, remark, recordDate }
  PasswordDTO:     { oldPassword, newPassword }
  BatchDeleteDTO:  { ids: Long[] }

VO（View Object，后端 → 前端）
  UserVO:                  { id, username, role, createTime, updateTime }
  RecordVO:                { id, userId, categoryId, categoryName, categoryType,
                             amount, remark, recordDate, createTime, updateTime }
  MonthlyStatisticsVO:     { year, month, totalIncome, totalExpense, balance }
  CategoryStatisticsVO:    { categoryId, categoryName, amount, percentage }
  AdminOverviewVO:         { totalUsers, totalRecords, totalCategories, totalIncome, totalExpense }
  AdminRecordVO:           { id, userId, username, categoryId, categoryName, categoryType,
                             amount, remark, recordDate, createTime }
```

---

## 十、实现进度跟踪

```
阶段一（基础 CRUD）
  ☐ 1. 项目初始化 + 数据库建表
  ☐ 2. 用户注册（密码加密）
  ☐ 3. 用户登录（JWT 生成）
  ☐ 4. JWT 拦截器（校验 + 提取用户）
  ☐ 5. 查看个人信息
  ☐ 6. 修改密码
  ☐ 7. 分类 CRUD

阶段二（账单管理）
  ☐ 8. 账单新增（含外键校验）
  ☐ 9. 账单修改 + 删除
  ☐ 10. 账单分页查询（多条件 + 联表）
  ☐ 11. 账单详情查询（联表）

阶段三（统计分析）
  ☐ 12. 月度收支汇总（SQL 聚合）
  ☐ 13. 分类统计（GROUP BY + 百分比）
```

> ✅ = 已完成 &nbsp; ☐ = 未开始 &nbsp; 🔄 = 进行中

### 测试验证

每个接口实现后，可通过以下方式验证：

1. **Swagger UI**：集成 `springdoc-openapi` 后访问 `http://localhost:8080/swagger-ui.html`
2. **Postman / Apifox**：导入接口文档手动测试
3. **前端联调**：启动前端项目 `npm run dev`，观察界面是否正常工作
