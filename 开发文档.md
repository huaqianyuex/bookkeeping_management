# 个人记账本 - 开发文档

## 项目概述

单页面应用，同时提供**桌面管理端**和**移动端 App 体验**两种界面，共享同一个后端 API。

- **管理端**：Element Plus 组件库，侧边栏布局，适合 PC 后台管理
- **移动端**：Vant 4 组件库，底部 Tab 导航，App 级交互（下拉刷新、左滑删除、堆栈导航）

---

## 技术栈

| 类别 | 技术 | 版本 |
|------|------|------|
| 框架 | Vue 3 (Composition API + `<script setup>`) | ^3.4.21 |
| 语言 | TypeScript | ^5.4.2 |
| 构建 | Vite | ^5.2.0 |
| 管理端 UI | Element Plus | ^2.6.3 |
| 移动端 UI | Vant | ^4.9.24 |
| 图标 | @element-plus/icons-vue | ^2.3.1 |
| 路由 | Vue Router 4 | ^4.3.0 |
| 状态管理 | Pinia | ^2.1.7 |
| HTTP | Axios | ^1.6.8 |
| 日期 | dayjs | ^1.11.10 |

---

## 目录结构

```
front_end/
├── src/
│   ├── api/                    # 共享 API 层
│   │   └── index.ts
│   ├── stores/                 # 共享状态管理
│   │   └── user.ts
│   ├── types/                  # 共享类型定义
│   │   └── index.ts
│   ├── utils/                  # 共享工具
│   │   └── request.ts          # Axios 封装（含 JWT 拦截器）
│   ├── router/
│   │   └── index.ts            # 路由配置（admin + mobile 合并）
│   ├── mobile/                 # 移动端页面
│   │   ├── layouts/
│   │   │   └── MobileLayout.vue      # 底部 Tabbar + 导航栏
│   │   ├── views/
│   │   │   ├── Home.vue             # 首页（收支总览）
│   │   │   ├── Records.vue          # 账单列表
│   │   │   ├── AddRecord.vue        # 记一笔
│   │   │   ├── Categories.vue       # 分类管理
│   │   │   └── Statistics.vue       # 数据统计
│   │   └── Login.vue                # 移动端登录/注册
│   ├── layouts/                # 管理端布局
│   │   └── MainLayout.vue
│   ├── views/                  # 管理端页面
│   │   ├── Login.vue
│   │   ├── Records.vue
│   │   ├── Categories.vue
│   │   └── Statistics.vue
│   ├── App.vue                 # 根组件
│   ├── main.ts                 # 入口（注册 Element + Vant）
│   └── vite-env.d.ts
├── index.html
├── package.json
├── vite.config.ts
├── tsconfig.json
├── 接口文档.md
├── 开发文档.md                 # ← 本文档
└── README.md
```

---

## 路由设计

URL 前缀区分两端入口：

| 模式 | 路径 | 页面 | 说明 |
|------|------|------|------|
| **管理端** | `/login` | 管理端登录 | |
| | `/records` | 账单记录 | 需登录 |
| | `/categories` | 分类管理 | 需登录 |
| | `/statistics` | 数据统计 | 需登录 |
| | `/` | 重定向到 `/records` | 需登录 |
| **移动端** | `/m/login` | 移动端登录 | |
| | `/m/home` | 首页 | 需登录 |
| | `/m/records` | 账单列表 | 需登录 |
| | `/m/add` | 记一笔 | 需登录 |
| | `/m/categories` | 分类管理 | 需登录 |
| | `/m/statistics` | 数据统计 | 需登录 |
| | `/m` | 重定向到 `/m/home` | 需登录 |

路由守卫自动判断：
- 访问需登录页面且无 Token → 跳转对应端的登录页
- 已登录时访问登录页 → 跳转对应端的首页

---

## 快速开始

### 安装依赖

```bash
cd front_end
npm install
```

### 启动开发服务器

```bash
npm run dev
```

开发服务器在 `http://localhost:5173` 启动，API 自动代理到 `http://localhost:8080`。

访问不同入口：

```
管理端：http://localhost:5173/
移动端：http://localhost:5173/m/home
```

### 生产构建

```bash
npm run build
```

产物输出到 `dist/` 目录。

---

## 移动端页面详解

### MobileLayout 布局

底部 Tabbar 包含 5 个标签：

| 图标 | 标签 | 路由 | 功能 |
|------|------|------|------|
| home-o | 首页 | `/m/home` | 月度收支总览 |
| notes-o | 账单 | `/m/records` | 账单列表 |
| + (圆形主色按钮) | 记账 | `/m/add` | 新增记录 |
| bar-chart-o | 统计 | `/m/statistics` | 数据统计 |
| apps-o | 分类 | `/m/categories` | 分类管理 |

顶部 NavBar 显示当前页面标题，支持返回按钮。

### 首页 (Home.vue)

- **月度收支卡片**：渐变背景展示本月结余、收入、支出
- **快速操作网格**：记一笔 / 账单 / 统计 / 上月
- **支出分类 Top5**：本月支出最多的 5 个分类及占比
- 无数据时显示空状态 → 引导记第一笔

### 账单列表 (Records.vue)

- **月份/分类筛选**：双按钮切换
- **统计栏**：本月收入/支出小计
- **下拉刷新**：`van-pull-refresh` 顶部下拉
- **上拉加载**：`van-list` 滚动分页（每页 20 条）
- **左滑删除**：`van-swipe-cell` 左滑出现红色删除按钮
- **点击编辑**：跳转记账页并携带记录 ID

### 记账 (AddRecord.vue)

- **类型切换**：支出/收入 按钮组
- **金额输入**：数字键盘 + `¥` 前缀 + 自动两位小数
- **日期选择**：底部弹出 DatePicker
- **分类选择**：底部弹出 ActionSheet（按当前类型筛选）
- **备注输入**：可选的文本输入（最多 30 字）
- **编辑模式**：通过路由 query `?id=xx&edit=1` 进入编辑

### 分类管理 (Categories.vue)

- **支出/收入分组**：两个独立区块
- **添加按钮**：每个区块右侧有添加按钮
- **编辑**：点击分类弹出 Dialog 编辑名称
- **删除**：点击垃圾桶图标弹出确认框

### 数据统计 (Statistics.vue)

- **月份切换**：左右箭头切换月份
- **三栏汇总**：收入 / 支出 / 结余
- **分类详情**：支出和收入各一个区块，含进度条显示占比

### 移动端登录 (Login.vue)

- 深色渐变背景
- Tabs 切换 登录 / 注册
- 表单验证 + 加载状态
- 登录成功后跳转 `/m/home`

---

## 管理端页面详解

管理端沿用原 Element Plus 组件库，桌面端适配：

| 路由 | 页面 | 特点 |
|------|------|------|
| `/login` | 登录 | 玻璃态背景 + 浮动动画 |
| `/records` | 账单 | 表格 + 统计卡片 + 底部弹窗表单 |
| `/categories` | 分类 | 双列网格 + Dialog 表单 |
| `/statistics` | 统计 | 汇总卡片 + 分类进度条 + Element Plus Progress |

管理端也保留了移动端的响应式适配（`@media max-width: 767px`），在小屏下可临时使用。

---

## 共享层设计

### 状态管理 (Pinia)

`stores/user.ts`：
- `token` — 从 localStorage 读写
- `userInfo` — 用户信息
- 两端共享同一个 store，切换端时登录状态保持

### HTTP 请求 (Axios)

`utils/request.ts`：
- 自动附加 `Authorization: Bearer {token}` 请求头
- 响应拦截统一处理 401（清除 token 并跳转登录）
- 错误消息统一通过 `ElMessage` 展示

### API 接口 (api/index.ts)

按模块导出：

```
userApi       — 登录 / 注册 / 获取信息 / 修改密码
categoryApi   — CRUD
recordApi     — 分页查询 / CRUD
statisticsApi — 月度汇总 / 分类统计
```

### 类型定义 (types/index.ts)

```
ApiResponse   — 统一响应格式
PageResult    — 分页结果
CategoryVO    — 分类视图
RecordVO      — 账单视图
MonthlyStatsVO — 月度统计
CategoryStatVO — 分类统计
DTO 类型       — LoginDTO / RegisterDTO / RecordDTO 等
```

---

## 后端接口

与 Java Spring Boot 后端对接，基础路径 `http://localhost:8080`。

详细接口定义见 `接口文档.md`。

| 模块 | 路径 | 说明 |
|------|------|------|
| 用户 | `/api/user/*` | 注册、登录、信息、改密 |
| 分类 | `/api/categories/*` | CRUD |
| 账单 | `/api/records/*` | 分页查询 + CRUD |
| 统计 | `/api/statistics/*` | 月度汇总、分类统计 |

认证方式：JWT Token，放在请求头 `Authorization: Bearer {token}`。

---

## 构建与部署

### 构建产物

```bash
npm run build
```

输出到 `dist/`，包含两端所有代码。部署到任意静态服务器即可（Nginx、Apache、OSS 等）。

### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;

    root /path/to/dist;
    index index.html;

    # API 反向代理
    location /api/ {
        proxy_pass http://localhost:8080;
    }

    # SPA 路由处理
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

### Vite 代理配置（开发环境）

`vite.config.ts` 中已配置 `/api` 代理到 `http://localhost:8080`。

---

## 环境变量

可通过 `.env` / `.env.development` / `.env.production` 配置自定义变量（如 API 地址）。

---

## 备忘录

### 为移动端添加新页面

1. 在 `src/mobile/views/` 下创建 `.vue` 文件
2. 在 `src/router/index.ts` 的 mobile children 中添加路由
3. 如需 Tabbar 导航，在 `MobileLayout.vue` 的 `pageTitleMap` 中添加标题映射

### 为管理端添加新页面

1. 在 `src/views/` 下创建 `.vue` 文件
2. 在 `src/router/index.ts` 的 admin children 中添加路由
